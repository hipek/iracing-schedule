template(name='mySeriesDetailsIndex')
  +contentFor "headerButtonLeft"
    button(class="button button-clear pull-left" data-ion-menu-toggle="left")
      +ionIcon icon="navicon"

  +contentFor "headerTitle"
    h1(class="title")
      | iSchedule
      | - 
      = team.name
      = season.name

  +ionView
    +ionSubheaderBar
      h2(class="title")
        = name

    +ionSubheaderBar
      h2(class="title")
        = name

    +ionContent
      +ionList
        +seriesDetails
